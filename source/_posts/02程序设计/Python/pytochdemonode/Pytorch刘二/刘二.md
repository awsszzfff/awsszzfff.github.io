---
date: 2001-01-01
tags:
  - Others
categories:
  - Others
title: "刘二"
---
pytorch0.4
空间映射，把N维空间中的东西映射到指定空间中，同时尽量保持原空间中物体的表示信息。（获取数据时）例如：将10维空间映射到3维空间。通过线性代数可实现空间的映射，例如将Nx1的向量映射到3x1的向量中，即通过寻找一个3xN维的向量进行相乘，可完成映射。
降维，尽量降低数据量，从而减少一定的开销（时间、金钱等）。

1. Dataset构建数据集
2. Model选择模型（eg：线性模型Linear Model，y=wx+b）
3. Training训练
4. inferring推理
用测试集来判断训练出来模型的优劣

Training set  Test set
训练集 测试集
训练集(x,y) 验证集/开发集(x,y) 测试集(x)（在测试之前，想要在自己训练出来的模型上进行测试调试，可用验证集【从原本的测试集上随机分离出来的一部分数据来进行自我测试】）

过拟合，训练集上表现很好，但同时学习到了很多噪声，导致测试集上表现一般/较差。
泛化能力，在没有见过的数据上（eg：图像）依旧能表现较好的处理（eg：识别）效果。



$cost=\frac1N\sum_{n=1}^N(\hat{y}_n-y_n)^2$MSE（Mean Square Error）
符号表示：预测结果y_hat


visdom训练数据可视化工具